## Usage
Run scripts in order:
1. `python -m scripts.get_sitemap` - generates 'sitemaps.json' FROM 'sites.txt'
2. `python -m scraper.parse_sitemaps` - generates 'articles.json' FROM 'sitemaps.json'
3. `python -m scraper.get_police_archives` - generates 'police_archives.txt' FROM 'police_sites.txt'
